- Add timer every 5/10s to check if dirs are mounted
- If all are mounted, hide the gcc
- If at least one is not mounted, show the gcc
- Mount only those which are not mounted yet
- Add to config decrypt_cmd (%ENC_DIR% %MOUNT_POINT%) with default of enc_fs without allow_other
